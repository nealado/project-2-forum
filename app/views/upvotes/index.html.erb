<p id="notice"><%= notice %></p>

<h1>Listing Upvotes</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Topic</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @upvotes.each do |upvote| %>
      <tr>
        <td><%= upvote.user %></td>
        <td><%= upvote.topic %></td>
        <td><%= link_to 'Show', upvote %></td>
        <td><%= link_to 'Edit', edit_upvote_path(upvote) %></td>
        <td><%= link_to 'Destroy', upvote, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
